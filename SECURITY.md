# Security Policy

Report vulnerabilities privately to the maintainers (see GitHub Security tab once published).
Do not open public issues for suspected secret leakage, auth bypass, or sandbox escape.

Guarantees we hold (see `docs/SECURITY.md`): approval-gated PRs, no default-branch writes,
network-isolated sandbox, secret redaction, encrypted tokens, verified webhooks.
